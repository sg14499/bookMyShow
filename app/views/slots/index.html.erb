<h1>Slots</h1>

<ul>
  <% @slots.each do |slot| %>
    <li>
      <a href="slots/<%= slot.id %>">
      <p>slot id : <%= slot.id %></p>
      <p>slot time : <%= slot.slot_time %></p>
    </li>
  <% end %>
</ul>


